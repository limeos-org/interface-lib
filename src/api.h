#pragma once
#include "all.h"

/** A type representing the public API of the interface library. */
struct InterfaceAPI
{
    // Placeholder
    void (*placeholder)(void);
};

/** The global instance of the interface library API. */
extern const struct InterfaceAPI interface;
